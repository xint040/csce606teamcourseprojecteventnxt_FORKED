<%# following changes are made:
        - Changed the first line to reflect the relationship between parent and child resources:
        - deleted the section that lists the parent_id of the related parent, 
          since this is not essential information in the view. %>

<div style="display: flex; justify-content: center; align-items: center;">
<%= form_with(model: [@event, seat]) do |form| %>
  <% if seat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
        <% seat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div style="margin-bottom: 10px;">
    <%= form.label :category, style: "display: block;" %>
    <%= form.text_field :category, style: "width: 100%; padding: 5px; border: 1px solid #ccc; border-radius: 5px;" %>
  </div>

  <div style="margin-bottom: 10px;">
    <%= form.label :section, style: "display: block;" %>
    <%= form.text_field :section, style: "width: 100%; padding: 5px; border: 1px solid #ccc; border-radius: 5px;" %>
  </div>

  <div style="margin-bottom: 10px;">
    <%= form.label :total_count, style: "display: block;" %>
    <%= form.number_field :total_count, style: "width: 100%; padding: 5px; border: 1px solid #ccc; border-radius: 5px;" %>
  </div>

  <div>
    <%= form.submit style: "width: 100%; padding: 10px; background-color: #4caf50; color: white; border: none; border-radius: 5px;" %>
  </div>
<% end %>
</div>
