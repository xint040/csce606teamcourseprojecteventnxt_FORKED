The commands to push code to heroku:

#To deploy the app on heroku, first create a Heroku account. You can find instructions to get a free student account online or in course materials.
#on command line or cloud 9 environment whichever you are using, go to the main application folder 
cd EventNXT-606/
heroku login
#to enter credential on command line itself, run below one
#heroko login --interative
heroku create app_name
#give appropriate app name. that will become the url name
git push heroku main
heroku run rake db:migrate
heroku run rake db:seed
#this should deploy the app on heroku account


# to deploy the app on client's heroku account, contact the client to get latest heroku login credentials.
